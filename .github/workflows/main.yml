name: Vprofile
on: workflow_dispatch
jobs:
  Testing:
    runs-on: ubuntu-latest
    steps:
      -  name: code checkout
         run: actions/checkout@v4
         
      -  name: maven test
         run: mvn test
